scalar DateTime
scalar Upload

type AuthResponse {
  email: String!
  token: String!
}

type Query {
  dummy: AuthResponse
}

type Mutation {
  login: AuthResponse
}

#include category.graphql
#include article.graphql
